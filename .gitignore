# .gitignore

# Terraform — never commit state or lock files with secrets
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfvars
terraform/override.tf

# Never commit real secret values
*.env
.env*

# OS
.DS_Store
*.swp
*~
